import { filterLinks } from './shared/filter-links.js'; const input=document.querySelector('#links');document.querySelector('#send').addEventListener('click',()=>{document.querySelector('#status').textContent=`${filterLinks(input.value.split(/\r?\n/)).length} links ready`;});
