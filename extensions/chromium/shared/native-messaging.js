export function frameMessage(message){const bytes=new TextEncoder().encode(JSON.stringify(message));const frame=new Uint8Array(4+bytes.length);new DataView(frame.buffer).setUint32(0,bytes.length,true);frame.set(bytes,4);return frame}export const refusal='Cookies and credentials are never forwarded.'