import type { HostAdapter, ResolvedLink, ResolutionPolicy } from "./adapter-types"; import { directHttpAdapter } from "./direct-http-adapter"; import { fuckingFastAdapter } from "./fuckingfast-adapter";
export class AdapterRegistry{private readonly adapters:HostAdapter[];constructor(adapters:HostAdapter[]=[fuckingFastAdapter,directHttpAdapter]){this.adapters=adapters;}forUrl(raw:string):HostAdapter{const url=new URL(raw);return this.adapters.find(adapter=>adapter.matches(url))??directHttpAdapter;}async resolve(raw:string,policy:ResolutionPolicy={cookiePolicy:"refuse",credentialPolicy:"refuse"}):Promise<ResolvedLink[]>{return this.forUrl(raw).resolve({url:raw,policy});}list():Pick<HostAdapter,"id"|"label">[]{return this.adapters.map(({id,label})=>({id,label}));}}
