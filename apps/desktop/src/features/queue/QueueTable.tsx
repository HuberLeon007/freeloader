import type{QueueItem}from'./queue-types';export function QueueTable({items,selectedIds,onSelect,onSelectAll,onRemove}:{items:QueueItem[];selectedIds:string[];onSelect:(id:string)=>void;onSelectAll:()=>void;onRemove:(id:string)=>void}){return <div role="table" aria-label="Downloads"><div role="row">{['Select','Name','Status','Progress','Size','Speed','Actions'].map((label,i)=><div role="columnheader" key={label}>{i===0?<input type="checkbox" aria-label="Select all downloads" onChange={onSelectAll}/>:label}</div>)}</div>{items.map(item=>{const percent=item.total&&item.total>0?Math.round(item.downloaded/item.total*100):item.status==='completed'?100:0;return <div role="row" key={item.id}><div role="cell"><input type="checkbox" aria-label={`Select ${item.name}`} checked={selectedIds.includes(item.id)} onChange={()=>onSelect(item.id)}/></div><div role="cell">{item.name}</div><div role="cell">{item.status}</div><div role="cell"><progress max="100" value={percent} aria-valuenow={percent} aria-label={`${item.name} progress`}/></div><div role="cell">{item.downloaded}</div><div role="cell">{item.speed}</div><div role="cell"><button type="button" aria-label={`Remove ${item.name}`} onClick={()=>onRemove(item.id)}>Remove</button></div></div>})}</div>}