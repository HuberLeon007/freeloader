import type { QueueItem } from './queue-types';
import { QueueRow } from './QueueRow';
export function QueueTable({items,selectedIds,onSelect,onSelectAll,onRemove}:{items:QueueItem[];selectedIds:string[];onSelect:(id:string)=>void;onSelectAll:()=>void;onRemove:(id:string)=>void}){return <div role="table" aria-label="Downloads"><div role="row" className="queue-head"><div role="columnheader"><input type="checkbox" aria-label="Select all downloads" checked={items.length>0&&selectedIds.length===items.length} onChange={onSelectAll}/></div>{['Name','Status','Progress','Size','Speed','Actions'].map(label=><div role="columnheader" key={label}>{label}</div>)}</div>{items.map(item=><QueueRow key={item.id} item={item} selected={selectedIds.includes(item.id)} onSelect={()=>onSelect(item.id)} onRemove={()=>onRemove(item.id)}/>)}</div>}
