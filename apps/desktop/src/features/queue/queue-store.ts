import{create}from'zustand';import type{QueueItem,QueueSnapshot}from'./queue-types';type State=QueueSnapshot&{add:(item:QueueItem)=>void;remove:(id:string)=>void;toggleSelected:(id:string)=>void;selectAll:()=>void;saveSnapshot:()=>QueueSnapshot;loadSnapshot:(snapshot:QueueSnapshot)=>void};const empty:QueueSnapshot={items:[],batches:[],selectedIds:[],settings:{density:'compact',concurrency:3}};export const useQueueStore=create<State>((set,get)=>({...empty,add:item=>set(state=>({items:[item,...state.items]})),remove:id=>set(state=>({items:state.items.filter(item=>item.id!==id),selectedIds:state.selectedIds.filter(item=>item!==id)})),toggleSelected:id=>set(state=>({selectedIds:state.selectedIds.includes(id)?state.selectedIds.filter(item=>item!==id):[...state.selectedIds,id]})),selectAll:()=>set(state=>({selectedIds:state.items.map(item=>item.id)})),saveSnapshot:()=>{const{items,batches,selectedIds,settings}=get();return{items,batches,selectedIds,settings}},loadSnapshot:snapshot=>set(snapshot)}))