export type CandidateLink={line:number;original:string;url:string|null;host:string|null;valid:boolean;reason?:string};
export function parsePaste(text:string):CandidateLink[]{const seen=new Set<string>();return text.split(/\r?\n/).map((raw,index)=>{const original=raw.trim();if(!original)return {line:index+1,original,url:null,host:null,valid:false,reason:"empty"};try{const parsed=new URL(original);if(parsed.protocol!=="http:"&&parsed.protocol!=="https:")throw new Error("unsupported protocol");const url=parsed.toString();if(seen.has(url))return {line:index+1,original,url,host:parsed.hostname,valid:false,reason:"duplicate"};seen.add(url);return {line:index+1,original,url,host:parsed.hostname,valid:true};}catch{return {line:index+1,original,url:null,host:null,valid:false,reason:"invalid URL"};}});}
export function validCandidates(text:string):CandidateLink[]{return parsePaste(text).filter(candidate=>candidate.valid);}
