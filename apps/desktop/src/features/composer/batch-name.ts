import type{CandidateLink}from'./paste-parser';export function proposeBatchName(items:CandidateLink[]){const counts=new Map<string,number>();for(const item of items)if(item.valid&&item.host)counts.set(item.host,(counts.get(item.host)||0)+1);const host=[...counts.entries()].sort((a,b)=>b[1]-a[1]||a[0].localeCompare(b[0]))[0]?.[0];return host?`${host} downloads`:'New downloads'}