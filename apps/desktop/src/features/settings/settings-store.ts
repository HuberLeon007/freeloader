import{create}from'zustand';export type Density='compact'|'comfortable'|'spacious';type State={density:Density;concurrency:number;defaultDirectory:string;rememberPrompt:boolean;setDensity:(value:Density)=>void;setConcurrency:(value:number)=>void};export const useSettingsStore=create<State>(set=>({density:'compact',concurrency:3,defaultDirectory:'',rememberPrompt:true,setDensity:density=>set({density}),setConcurrency:concurrency=>set({concurrency:Math.max(1,Math.min(8,Math.round(concurrency)))})}))