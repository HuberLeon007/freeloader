import type{Density}from'./settings-store';export function SettingsDialog({open,density,concurrency,onDensityChange,onConcurrencyChange,onClose}:{open:boolean;density:Density;concurrency:number;onDensityChange:(v:Density)=>void;onConcurrencyChange:(v:number)=>void;onClose:()=>void}){if(!open)return null;return <div role="dialog" aria-modal="true" aria-labelledby="settings-title"><h2 id="settings-title">Settings</h2><fieldset><legend>Density</legend>{(['compact','comfortable','spacious'] as const).map(v=><label key={v}><input type="radio" checked={density===v} onChange={()=>onDensityChange(v)}/>{v}</label>)}</fieldset><label>Concurrency <input type="number" min={1} max={8} value={concurrency} onChange={e=>onConcurrencyChange(Number(e.target.value))}/></label><button type="button" onClick={onClose}>Done</button></div>}