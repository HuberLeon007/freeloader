import { DensityToggle } from './DensityToggle'; import { ConcurrencyControl } from './ConcurrencyControl'; import type { Density } from './settings-store';
export function SettingsDialog({open,density,concurrency,onDensityChange,onConcurrencyChange,onClose}:{open:boolean;density:Density;concurrency:number;onDensityChange:(value:Density)=>void;onConcurrencyChange:(value:number)=>void;onClose:()=>void}){if(!open)return null;return <div role="dialog" aria-modal="true" aria-labelledby="settings-title"><h2 id="settings-title">Settings</h2><DensityToggle value={density} onChange={onDensityChange}/><ConcurrencyControl value={concurrency} onChange={onConcurrencyChange}/><button type="button" aria-label="Close settings" onClick={onClose}>Done</button></div>}
